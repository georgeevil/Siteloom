# Dev/test dependencies (pytest + httpx test client, coverage, lint, types).
# Includes the runtime set.
#   .venv/bin/pip install -r requirements-dev.txt
#
# The gates all live here rather than in a separate tools file, so that a checkout
# that can run the suite can also run everything CI runs:
#   .venv/bin/pytest                 # tests + coverage (see pyproject [tool.coverage])
#   .venv/bin/ruff check .           # lint
#   .venv/bin/mypy                   # types, per-module opt-in
# Ranges live in pyproject.toml [project.optional-dependencies].dev; these are the pins.

-r requirements.txt

ast_serialize==0.8.0
coverage==7.15.4
iniconfig==2.3.0
librt==0.15.0
mypy==2.3.0
mypy_extensions==1.1.0
pathspec==1.1.1
pluggy==1.6.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.2
